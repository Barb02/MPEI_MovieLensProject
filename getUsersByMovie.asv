function usersByMovie = getUsersByMovie(dicInfoByUser,NumUsers,filmID)    
    usersByMovie = {};
    Set = dicInfoByUser{:,4};
    for user = 1:NumUsers
        movies = Set{user,1};
        if find(movies(:,1) == filmID) > 0
            usersByMovie{end+1} = {user dicInfoByUser{2} d};
        end
    end
end

